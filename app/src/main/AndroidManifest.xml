<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.maruge.maruge_comunicados">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".Administrador.postagens" />
        <activity android:name=".Administrador.nova_postagem" />
        <activity android:name=".novo_usuario" />
        <activity android:name=".Administrador.menu_adm" />
        <activity android:name=".Usuario.Menu_Usuario" />
        <activity android:name=".Usuario.PostagemUsuario" />
        <activity android:name=".Administrador.ListaUsuario" />
        <activity android:name=".Usuario.Configurar" />
        <activity android:name=".Usuario.EditarUsuario"></activity>
    </application>

</manifest>